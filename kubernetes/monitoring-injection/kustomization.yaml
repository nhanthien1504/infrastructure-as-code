# Thư mục này chứa các patch Kustomize liên quan đến monitoring.
# Job CI/CD `deploy-k8s.yml` sẽ tự động tạo ra các patch cần thiết
# dựa trên các biến môi trường, thay vì sử dụng các file tĩnh ở đây.
